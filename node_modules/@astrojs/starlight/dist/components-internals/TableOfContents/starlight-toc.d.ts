//#region src/components-internals/TableOfContents/starlight-toc.d.ts
declare class StarlightTOC extends HTMLElement {
  private _current;
  private minH;
  private maxH;
  /**
   * CSS selector string that matches only headings that can appear in the table of contents.
   * Generates a selector like `h1#_top,:where(h2,h3)[id]`.
   */
  private tocHeadingSelector;
  protected set current(link: HTMLAnchorElement);
  private onIdle;
  constructor();
  private init;
  private getRootMargin;
}
//#endregion
export { StarlightTOC };