import { z } from "astro/zod";
//#region src/schemas/expressiveCode.ts
const ExpressiveCodeSchema = () => z.union([z.custom((value) => typeof value === "object" && value), z.boolean()]).optional();
//#endregion
export { ExpressiveCodeSchema };
